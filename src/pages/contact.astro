---
import ProseLayout from "../layouts/ProseLayout.astro";


const title = "Contact Us";
const description = "Contact Form";
---
<ProseLayout title={title} description={description}>
    <section>
        
    
        <form action="https://formspree.io/f/xblrvyvo"
        method="POST" id="contactForm" class="subscribe-content contact-form">
          <div class="inputWrapper">
            <input type="email" name="email" id="email" class="input" placeholder="Your Email" required>
          </div>
          <div class="inputWrapper">
            <input id="subject" type="text" name="name" class="input" placeholder="Subject" >
          </div>
          
          
    
          <div class="inputWrapper">
            <textarea name="message" id="message" class="input message-area" placeholder="Your Message" rows="5" required></textarea>
          </div>
    
          <div class="inputWrapper">
            <button type="submit" class="iconButton">Send</button>
          </div>
          
    
          <div class="disclaimer">
            We respect your privacy. Your information is safe with us.
          </div>
          <div class="disclaimer">
            <span id="successMessage"></span>
            <span id="errorMessage"></span>
          </div>
        </form>
      </section>

  <style lang="scss">

.contact-form {
    display: flex;
    flex-direction: column;
    max-width: 600px;
    gap: var(--space-m);
    margin-inline: auto;
    position: relative;
}



.inputWrapper {
  position: relative;
  width: 100%;
  margin-bottom: var(--space-s);
}

.input {
  color: var(--gray-12);
  background: var(--gray-1);
  padding: var(--space-l);
  border: 2px solid var(--gray-12);
  border-radius: var(--radius-full);
  width: 100%;
  font-size: var(--step-0);
}

.message-area {
    min-height: 300px;
    resize: none;
    border-radius: 25px;
}

.input::placeholder {
  color: var(--gray-10);
  opacity: 0.7;
}

.input:focus {
  outline: 3px solid var(--amber-a5test);
  outline-offset: 2px;
}

.iconButton {
  background: var(--gray-12);
  color: var(--gray-1);
  padding: 12px 24px;
  border-radius: 10px;
  border: none;
  cursor: pointer;
  transition: background 0.3s;
  width: 100%;
  text-align: center;
  font-size: var(--step-0);
}

.iconButton:hover {
  background: var(--gray-10);
}

.iconButton:disabled {
  background: var(--gray-9);
  pointer-events: none;
}

.disclaimer {
  color: var(--gray-11);
  margin-top: var(--space-m);
  font-size: 0.875rem;
  text-align: center;
}




  </style>
</ProseLayout>

